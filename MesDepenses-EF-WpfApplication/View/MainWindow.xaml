<Window x:Class="MesDepenses_EF_WpfApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MesDepenses_EF_WpfApplication"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <DataGrid AutoGenerateColumns="False" x:Name="dataGridListHotels" HorizontalAlignment="Left" Margin="46,47,0,0" VerticalAlignment="Top"  ItemsSource="{Binding Personnes}" Height="244" Width="439">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID Personne" Binding="{Binding PersonneID}" />
                <DataGridTextColumn Header="Nom de la personne" Binding="{Binding Nom}" />
                <DataGridTextColumn Header="Prenom de la personne" Binding="{Binding Prenom}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
